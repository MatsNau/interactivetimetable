<Window x:Class="Timetable.App.CalendarDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ferien &amp; externe Events"
        Width="540" Height="460" MinWidth="440" MinHeight="320"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Window.Resources>
        <Style x:Key="EntryGrid" TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="CanUserDeleteRows" Value="False" />
            <Setter Property="SelectionMode" Value="Single" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HorizontalGridLinesBrush" Value="#E0E0E0" />
            <Setter Property="RowHeight" Value="24" />
            <Setter Property="Background" Value="White" />
        </Style>
    </Window.Resources>
    <DockPanel Margin="14">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="OK" IsDefault="True" Padding="16,5" Margin="0,0,8,0" Click="Save_Click" />
            <Button Content="Abbrechen" IsCancel="True" Padding="12,5" />
        </StackPanel>

        <TabControl>
            <TabItem Header="Ferien">
                <DockPanel Margin="8">
                    <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,8,0,0">
                        <Button Content="Hinzufügen" Padding="12,5" Margin="0,0,8,0" Command="{Binding AddHolidayCommand}" />
                        <Button Content="Entfernen" Padding="12,5" Foreground="#B22222" Command="{Binding RemoveHolidayCommand}" />
                    </StackPanel>
                    <DataGrid Style="{StaticResource EntryGrid}"
                              ItemsSource="{Binding Holidays}" SelectedItem="{Binding SelectedHoliday}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Width="*"
                                                Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
                            <DataGridTextColumn Header="Von" Width="90"
                                                Binding="{Binding StartText, UpdateSourceTrigger=PropertyChanged}" />
                            <DataGridTextColumn Header="Bis" Width="90"
                                                Binding="{Binding EndText, UpdateSourceTrigger=PropertyChanged}" />
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </TabItem>

            <TabItem Header="Externe Events">
                <DockPanel Margin="8">
                    <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,8,0,0">
                        <Button Content="Hinzufügen" Padding="12,5" Margin="0,0,8,0" Command="{Binding AddEventCommand}" />
                        <Button Content="Entfernen" Padding="12,5" Foreground="#B22222" Command="{Binding RemoveEventCommand}" />
                    </StackPanel>
                    <DataGrid Style="{StaticResource EntryGrid}"
                              ItemsSource="{Binding Events}" SelectedItem="{Binding SelectedEvent}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Width="*"
                                                Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
                            <DataGridTextColumn Header="Von" Width="90"
                                                Binding="{Binding StartText, UpdateSourceTrigger=PropertyChanged}" />
                            <DataGridTextColumn Header="Bis" Width="90"
                                                Binding="{Binding EndText, UpdateSourceTrigger=PropertyChanged}" />
                            <DataGridCheckBoxColumn Header="Hervorheben" Width="Auto"
                                                    Binding="{Binding IsHighlighted, UpdateSourceTrigger=PropertyChanged}" />
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
